import { Text } from "react-native";
import SchduleReminder from "../../components/SchduleReminder";

export default function SettingsScreen({ navigation }) {

    return (
        <SchduleReminder />
    )
}